<?php// teams/api/fetch_teams.phpheader('Content-Type: application/json');session_start();if (!isset($_SESSION['user_id']) || $_SESSION['role'] !== 'lecturer') {    http_response_code(401);    echo json_encode(['success' => false, 'error' => 'Unauthorized']);    exit;}require_once __DIR__ . '/../../db.php';require_once __DIR__ . '/../controllers/TeamController.php';$assessmentId = (int)($_GET['assessment_id'] ?? 0);$page = max(1, (int)($_GET['page'] ?? 1));if ($assessmentId <= 0) {    http_response_code(400);    echo json_encode(['success' => false, 'error' => 'Missing assessment_id']);    exit;}try {    $controller = new TeamController($pdo);    $result = $controller->getTeamsForAssessment($assessmentId, $page);    echo json_encode($result);} catch (Exception $e) {    http_response_code(400);    echo json_encode(['success' => false, 'error' => $e->getMessage()]);}